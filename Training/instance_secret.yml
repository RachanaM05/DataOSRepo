# PostgreSQL Read Instance-secret Manifest

name: postgresdepotmr-r # Unique identifier for Resource, replace ${DEPOT_NAME} with depot name
version: v1 # Manifest version
type: instance-secret # Type of the Resource
description: Read-only secret for Postgres database # Purpose of the Instance-secret
layer: user # DataOS layer
instance-secret:
  type: key-value-properties # Secret type
  acl: r # Access control: 'r' for read-only
  data:
    username: postgres # replace with postgres username
    password: DyajkiQTz78qwnixgQolTw1rVr # replace with postgres password